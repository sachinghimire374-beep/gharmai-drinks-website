# Netlify configuration for Gharmai Drinks (Next.js 15 + Prisma)
[build]
  command = "npm run build"
  publish = ".next"

# Official Next.js runtime — handles SSR, API routes, image optimization, middleware.
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
